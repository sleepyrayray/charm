---
import { navigation as enNavigation, site as enSite } from "../../data/en/site";
import { navigation as frNavigation, site as frSite } from "../../data/fr/site";

interface Props {
  locale?: "en" | "fr";
  currentPath: string;
}

const { locale = "en", currentPath } = Astro.props;
const site = locale === "fr" ? frSite : enSite;
const navigation = locale === "fr" ? frNavigation : enNavigation;
const quoteHref = locale === "fr" ? "/fr/quote" : "/quote";

const switchHref = locale === "fr"
  ? currentPath.replace(/^\/fr/, "") || "/"
  : `/fr${currentPath === "/" ? "" : currentPath}`;
---

<header class="sticky top-0 z-40 px-3 py-3 sm:px-4">
  <div class="shell">
    <div class="panel flex items-center justify-between rounded-lg px-4 py-3 sm:px-6">
      <a href={locale === "fr" ? "/fr" : "/"} class="min-w-0">
        <div class="text-sm font-semibold text-[var(--primary-strong)]">{site.brand}</div>
        <div class="truncate text-xs text-[var(--muted)]">{site.subtext}</div>
      </a>

      <nav class="hidden items-center gap-2 md:flex">
        {navigation.map((item) => {
          const active = currentPath === item.href;

          return (
            <a
              href={item.href}
              aria-current={active ? "page" : undefined}
              class:list={[
                "rounded-lg px-4 py-2 text-sm font-medium transition",
                active ? "bg-[var(--primary-soft)] text-[var(--primary-strong)]" : "text-[var(--ink)] hover:bg-white",
              ]}
            >
              {item.label}
            </a>
          );
        })}
      </nav>

      <div class="hidden items-center gap-3 md:flex">
        <a href={quoteHref} class="button-primary">{site.quoteLabel}</a>
        <a href={switchHref} class="rounded-lg border border-[var(--line)] px-3 py-2 text-xs font-semibold text-[var(--muted)]">{site.languageLabel}</a>
      </div>

      <div class="flex items-center gap-2 md:hidden">
        <a href={switchHref} class="rounded-lg border border-[var(--line)] px-3 py-2 text-xs font-semibold text-[var(--muted)]">{site.languageLabel}</a>
        <a href={quoteHref} class="button-primary px-4">{site.quoteLabel}</a>
      </div>
    </div>

    <nav class="mt-3 flex flex-wrap gap-2 md:hidden">
      {navigation.map((item) => {
        const active = currentPath === item.href;

        return (
          <a
            href={item.href}
            aria-current={active ? "page" : undefined}
            class:list={[
              "rounded-lg px-4 py-2 text-sm font-medium transition",
              active
                ? "bg-[var(--primary-soft)] text-[var(--primary-strong)]"
                : "panel text-[var(--ink)]",
            ]}
          >
            {item.label}
          </a>
        );
      })}
    </nav>
  </div>
</header>
